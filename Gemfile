source 'https://rubygems.org/'
gemspec

gem 'ritual'
gem 'rspec', '3.1.0'
# are we still supporting rbx? can we make the conditional differently?
# why is this impacting MRI ruby 3.0 ?
# gem 'rubysl', platform: :rbx

group :dev do
  gem 'pry'
end
